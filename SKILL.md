---
name: agent-project-map
description: 为任意项目生成「项目地图 + 六层断言守护（防漂移、防地图饿死、防语义欠账）」，根治 AI 大项目开发中的失忆与幻觉。当地图建成，AI 接手任何任务都能秒定位目录职责、启动方式、部署真相与历史陷阱。触发词：项目地图、生成地图、scan project、project map、agent map、文档索引、AI 失忆、上下文失控、大项目幻觉、记忆宫殿。
---

<!--
[变更日志]
修改时间：2026-09-17
AI模型：GLM (CodeBuddy)
修改内容：[v1.0：设计契约 6 条扩为 8 条（#5 分层可再生、#6 待确认双机制、#7 产物区标注约定、#8 歧义标准）；Phase 1 新增准则文件探测默认值；Phase 4 常量区清单更新；Phase 5 三类裁决扩为四类（新增饿死风险）；Phase 6 规则 3 补产物区豁免代价说明；新增 Phase R 逃生舱；自检体系从四层升级为六层；技能更名 scan-project-map → agent-project-map（GitHub 查重零撞车，守护卖点前置至 description 首句）]
-->

# Scan Project Map — 项目地图生成器

你的任务：为目标项目生成 `project-map.md`（项目地图）+ `scripts/test_project_map.py`（六层自检脚本），并把地图接入该项目的 AI 行为准则文件。**地图是给 AI 用的外部记忆：只回答"在哪、什么任务去哪"，绝不回答"怎么实现、进展如何"。**

## 设计契约（八条红线，写图前必读，写图后写进地图头部防未来模型好心改坏）

1. **卖语义不卖结构**：AI 随时能列目录，结构是免费信息。地图唯一的商品是列目录拿不到的隐性知识——职责、读取时机、陷阱。**严禁贴完整文件树。**
2. **粒度锁目录级**：只写目录职责 + 5~8 个入口文件。日常文件级变更打不穿地图，只有结构性手术（目录增删移）才触发更新义务。维护成本是设计出来的，不靠自觉。
3. **只路由不复制**：凡有机器事实源的信息（网关/构建配置、package.json、建表 SQL、启动脚本），地图只写"真相在那里"，绝不复制内容。**尤其不写精确行数、文件数、占比等机器可秒算的数据。** 复制 = 制造第二事实源 = 制造未来冲突。
4. **权威分层**：地图的【结构声明】以文件系统为准（脚本校验）；【职责语义】在被用户推翻前有效（人守护）。这让过时地图只是降效，不会成为幻觉源。
5. **分层可再生**：【结构声明】可从文件系统重新推导，最坏情况是重写而非数据损失；【语义层】（职责、时机、陷阱）唯一事实源是人——整体重写前必须导出旧语义层由用户逐条裁决，严禁静默丢弃。
6. **禁止猜测 + 待确认双机制**：未查明的目录一律标【待确认】，编造语义比留盲区更毒。机器发现的（自检红灯建议追加的）用清单块内联 `# 【待确认】` 标记（补语义时删掉，超 5 条红灯）；人主动登记的走地图第 6 节。两处勿混用。
7. **产物区标注约定**：运行产物/资源目录在清单块内用 `# 产物区` 或 `# 资源区` **开头**的注释标注，自检脚本据此豁免存在性检查（fresh clone 无产物目录不算失效）。目录性质唯一事实源是地图清单，脚本常量 `IGNORE` 只放纯机械缓存。
8. **歧义标准**：机器只做无歧义的记账（已存在的事实可登记）；凡涉删除、豁免、保留历史的裁决一律红灯上浮给人。这是断言层与 Phase 6 "结构性记账可自主"的共同边界。

## 执行流程（七阶段 + 逃生舱，严格按序）

### Phase 0 — 前置确认
确认目标项目根目录；确认 Python 3 可用（跑自检脚本用）。若不可用，停下来与用户协商替代方案。

### Phase 1 — 自主扫描（先扫后问，问题才有质量）
- 列出项目 2~3 层目录结构（跳过 node_modules、__pycache__、dist、build、点目录）
- 读：README、依赖清单（package.json / requirements.txt / go.mod 等）、启动脚本、网关/路由配置（nginx.conf / vercel.json 等）、CI 配置
- **探测 AI 行为准则文件**（作为 Phase 2 第 2 问的默认值）：优先级 `AGENTS.md > CLAUDE.md > agent.md > .cursorrules`，多个并存时全部列出让用户选主入口；都没有则默认新建 AGENTS.md
- 识别：项目有几端/几模块、技术栈、入口文件、构建产物路径、部署配置分散在哪几处
- **扫描中发现的疑点（空目录、死路径残留、同名目录）记录下来，问卷时向用户求证**

### Phase 2 — 用户问卷（3~5 问，每问必须带默认值，允许用户全部用默认）
用结构化提问（编号列表或工具），核心问题：

1. **端构成确认**：「我扫描到项目包含 X 端/模块（列出），对吗？有没有我看不到的部分（如独立部署的脚本、外部服务）？」
2. **行为准则文件**：「你项目的 AI 指令文件是哪个？」——默认值取 Phase 1 探测结果；都没有则新建 AGENTS.md（否则地图没有强制读取的载体，会饿死）
3. **陷阱知识口述**：「有没有已知的坑想让我写进地图？（死路径、历史遗留目录、曾出过的事故、部署配置分散处）」——这是 AI 扫描不到的最高价值信息，也是地图语义层唯一不可再生的事实源
4. **动态信息边界**：「端口/环境等易变信息，写死还是标注'动态，以进度文档为准'？」（推荐后者）

### Phase 3 — 生成地图
复制 `templates/project-map.template.md` 到项目根目录改名为 `project-map.md`，按扫描结果与问卷答案填充。**硬性预算 ≤150 行**；地图语言跟随用户语言。

正文结构（六个小节，编号从 0 开始）：
0. **项目规模速览（可选）**：只写端级量级与技术栈（如"桌面端 Tauri Rust + React TS，约 X 端"），不写精确行数/文件数；可选 ASCII 端级边界图，**明确标注"只画端级边界，不画子模块"**
1. **机器可读清单**：一个 ```map-paths 围栏块（自检脚本唯一解析面），逐行声明全部结构性路径，每行格式"路径 # 注释"；运行产物/资源目录的注释必须以 `产物区` 或 `资源区` 开头（触发存在性豁免，见契约 7）
2. **端与端的边界**：流量如何分发到各端（路由真相源指向具体配置文件，此处仅概览）
3. **目录职责速查表**：**路径 | 职责 | 何时去 三列表格**，让 AI 直接拿到"什么任务该进这个目录"的判断
4. **启动路由**：每端启动命令（命令真相源指向 start 脚本/package.json）；端口等易变信息标注"动态，以进度文档为准"
5. **陷阱区**：AI 无法从文件系统自行推断的知识（死路径残留、同名目录、部署配置分散处、运行产物文件）
6. **待确认区**：仅登记人主动承认未查明的目录（机器红灯追加的走清单块内联【待确认】标记，勿写入本节）

### Phase 4 — 生成自检脚本
复制 `templates/test_project_map.template.py` 到项目 `scripts/` 目录（无则创建）改名 `test_project_map.py`，配置文件顶部的常量区（MAP_NAME、RULE_FILES、ACTIVE_PARENTS、IGNORE、AGENT_REFERENCES、PENDING_BUDGET）。**IGNORE 只放纯机械缓存**（node_modules、venv、dist 等），运行产物/资源区一律走清单块标注，勿回填名字黑名单。

### Phase 5 — 运行至全绿（预期管理：首跑大概率抓到东西）
```
python scripts/test_project_map.py
```
红灯逐个裁决，四类处理：
- **未登记的新目录** → 问用户是有效结构（登记入清单；运行产物/资源目录注释以 `产物区`/`资源区` 开头）还是残留（删除）。红灯输出里已附可复制答案行，粘贴后补语义即可
- **声明失效**（清单声明但不存在）→ 勘误路径或删除声明；若为运行产物，注释改为以 `产物区`/`资源区` 开头豁免
- **饿死风险**（断言五）→ 准则文件被重写丢掉了接入规则，重新执行 Phase 6
- **误报**（脚本配置问题）→ 调整脚本常量，绝不放水删断言

同时用 pytest 验证双模：`python -m pytest scripts/test_project_map.py -q`（若项目无 pytest 则跳过此步）。

### Phase 6 — 接入行为准则（只增不改不删）
在 Phase 2 确认的准则文件中追加四条，并遵守该项目自身的日志/格式规范：
1. **读取触发**（满足其一即读地图）：首次定位代码前 / 不确定目录用途时 / 结构性变更落地后
2. **结构性变更同步**：目录增删移落地时同步地图清单块并顺手跑自检脚本；语义级改写需先向用户简报确认，结构性记账可自主（边界见契约 8：机器只做无歧义记账，删除/豁免/保留历史上浮）
3. **收口闸门**：阶段性收尾必跑自检并扫读地图，红灯时禁止签署冻结时间戳（产物区豁免存在性检查是已知接受代价——永久删除的产物目录无信号，靠本条的"扫读"人工兜住）
4. **大版本归档**：大版本更迭时地图随核心文档一并归档，随新架构重写

### Phase 7 — 交付总结
向用户输出：文件清单、断言绿灯证据、首跑抓到的问题及裁决结果、待确认清单、后续维护规则一句话说明。可选交付：`templates/pre-commit.template.yaml`（防漂移 CI 片段，把收口闸门从自觉升级为流水线强制）。

### Phase R — 逃生舱（地图腐烂至不可救时）
地图严重漂移、自检红灯无法逐条裁决时：重新跑 Phase 1 扫描 + Phase 3 生成新地图，**旧地图的语义层（第 3 节职责表、第 5 节陷阱区、第 6 节待确认区）必须整段导出给用户逐条裁决后平移**——结构按新扫描重建，语义绝不静默丢弃（契约 5）。平移后对照新结构逐条复核，主体路径已不存在的条目标 `[可能已失效]` 交用户裁决。落地后重跑 Phase 5 至全绿。

## 防改坏警告（交付时必须告知用户）

任何模型未来想往地图里塞文件树、复制配置内容、追加规划、静默重写语义层——都是在破坏设计。地图头部的八条设计契约是修改前必读的防线；六层断言中的体积预算与语义欠账断言是最后护栏。
